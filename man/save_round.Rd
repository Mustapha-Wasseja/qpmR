% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rounds.R
\name{save_round}
\alias{save_round}
\alias{load_round}
\alias{list_rounds}
\title{Save, load, and list forecast rounds}
\usage{
save_round(round, store = "rounds", overwrite = FALSE)

load_round(name, store = "rounds")

list_rounds(store = "rounds")
}
\arguments{
\item{round}{A \code{qpm_round}.}

\item{store}{Directory of the round store (created if missing).}

\item{overwrite}{Allow replacing an existing round of the same name.}

\item{name}{Round name (or its slug), or a direct path to a round
directory or \code{round.rds}.}
}
\value{
\code{save_round()} returns the round directory invisibly;
\code{load_round()} returns the \code{qpm_round}; \code{list_rounds()} returns a
data frame of the store's contents.
}
\description{
A round store is a plain directory: each round lives in
\verb{store/<slug>/} as a self-contained \code{round.rds} plus human-readable
sidecars (\code{forecast.csv}, \code{data.csv}, \code{calibration.csv}, and
\code{judgment.csv} when judgment was applied) so that a round can be
audited without R.
}
\examples{
m <- qpm_template("bkl")
obs <- simulate(qpm_solve(m), nsim = 40, seed = 1, burn = 20)
obs$period <- next_quarters("2016-Q1", 40)
r <- qpm_round("demo", m, obs[, c("period", "pi", "i", "q")], horizon = 8)
store <- file.path(tempdir(), "rounds")
save_round(r, store)
list_rounds(store)
r2 <- load_round("demo", store)
}
