% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rounds.R
\name{qpm_round}
\alias{qpm_round}
\title{Forecast rounds: one replayable artifact per forecast}
\usage{
qpm_round(
  name,
  model,
  data,
  observables = NULL,
  horizon = 12,
  bands = c(0.5, 0.7, 0.9),
  measurement_error = 0,
  kappa = 1e+06
)
}
\arguments{
\item{name}{Round name, e.g. \code{"2026-Q3 September"}.}

\item{model}{A \code{qpm_model}.}

\item{data}{Data frame in levels with a \code{period} column; passed to
\code{\link[=qpm_filter]{qpm_filter()}}. Quarter labels (\code{"2026-Q3"} style) are required for
cross-round comparison.}

\item{observables}{Columns treated as observed (default: all columns
matching declared variables).}

\item{horizon}{Forecast horizon in quarters.}

\item{bands}{Fan coverage levels.}

\item{measurement_error, kappa}{Passed to \code{\link[=qpm_filter]{qpm_filter()}}.}
}
\value{
An object of class \code{qpm_round} with elements \code{model}, \code{data},
\code{solution}, \code{fit}, and \code{forecast}.
}
\description{
A forecast round binds everything that produced a forecast -- the
model and its calibration, the data vintage, the filtration, the
forecast with its conditions and judgment -- into one object that can
be saved, reloaded, re-run, and compared with later rounds via
\code{\link[=compare_rounds]{compare_rounds()}}. This is the object an institution archives: next
quarter, "why did the forecast move?" is answered from the rounds, not
from memory.
}
\details{
\code{qpm_round()} runs the standard pipeline (solve, filter, baseline
forecast). Conditions, scenarios, and judgment are then applied to the
round directly: \code{\link[=qpm_condition]{qpm_condition()}}, \code{\link[=qpm_scenario]{qpm_scenario()}}, and
\code{\link[=add_judgment]{add_judgment()}} all accept a round and update its forecast.
}
\examples{
m <- qpm_template("bkl")
sol <- qpm_solve(m)
obs <- simulate(sol, nsim = 40, seed = 1, burn = 20)
obs$period <- next_quarters("2016-Q1", 40)
r <- qpm_round("test round", m, obs[, c("period", "pi", "i", "q")],
               horizon = 8)
r
}
