% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rules.R
\name{qpm_rule_eval}
\alias{qpm_rule_eval}
\alias{plot.qpm_rule_eval}
\title{Evaluate alternative policy rules}
\usage{
qpm_rule_eval(model, grid, loss = c(pi = 1, y_gap = 0.5), diff_loss = NULL)

\method{plot}{qpm_rule_eval}(x, xvar = NULL, yvar = NULL, ...)
}
\arguments{
\item{model}{A \code{qpm_model}.}

\item{grid}{A data frame of parameter values, one row per rule and one
column per parameter (e.g. from \code{\link[=expand.grid]{expand.grid()}}).}

\item{loss}{Named weights on the variances of levels, e.g.
\code{c(pi = 1, y_gap = 0.5)}.}

\item{diff_loss}{Named weights on the variances of first differences,
e.g. \code{c(i = 0.5)} to penalise instrument volatility.}

\item{x}{A \code{qpm_rule_eval}.}

\item{xvar, yvar}{Axes of the frontier. Either a variable name, whose
level variance is used, or a scored column name directly — so
\code{xvar = "vard_i"} traces the classic trade-off against instrument
volatility. Defaults to the first two entries in \code{loss}.}

\item{...}{Unused.}
}
\value{
A data frame of class \code{qpm_rule_eval}: the grid, the variance
of each targeted variable, the loss, and the Blanchard-Kahn outcome.
}
\description{
Answers the question a policy committee actually asks — \emph{what if we
responded differently?} — by re-solving the model over a grid of rule
parameters and scoring each one by the unconditional loss
\deqn{L = sum_v w_v var(v) + sum_v w^d_v var(v - v_{-1})}
computed from the model's stationary covariance rather than by
simulation, so it is exact. Tracing the resulting variance pairs gives
the inflation-output variability frontier (the Taylor curve).
}
\details{
Rules that violate Blanchard-Kahn are reported as such rather than
dropped: a policy response too weak to deliver determinacy is a
finding, not a missing row.
}
\examples{
m <- qpm_template("bkl")
grid <- expand.grid(c2 = c(1.2, 1.5, 2, 3), c3 = c(0, 0.5, 1))
ev <- qpm_rule_eval(m, grid, loss = c(pi = 1, y_gap = 0.5),
                    diff_loss = c(i = 0.5))
ev
plot(ev)
}
