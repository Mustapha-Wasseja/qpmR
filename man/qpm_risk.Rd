% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk.R
\name{qpm_risk}
\alias{qpm_risk}
\title{Express a balance of risks (skewed fan charts)}
\usage{
qpm_risk(fc, ..., author = "MPC", rationale = "")
}
\arguments{
\item{fc}{A \code{qpm_forecast}, or a \code{qpm_round} (its forecast is used).}

\item{...}{Named skews: one argument per variable, each a named vector
of \emph{mean minus mode} by period, e.g. \code{pi4 = c("2027-Q1" = 0.3)}.
A single unnamed value applies to every horizon.}

\item{author, rationale}{Recorded with the risk assessment, as for
judgement.}
}
\value{
The forecast with skewed bands. \verb{$risk} records the skews and
\verb{$paths} gains a \code{mode} column alongside \code{mean}.
}
\description{
Central banks rarely believe their fan charts are symmetric: the
published judgement is usually "risks to inflation are tilted to the
upside". \code{qpm_risk()} applies that judgement, replacing the Gaussian
bands of a forecast with two-piece normal bands whose \emph{mode} stays on
the model's projection while the \emph{mean} shifts by the stated skew.
Total uncertainty is preserved: the variance implied by the model at
each horizon is held fixed, so a skew redistributes risk rather than
adding it.
}
\details{
The skew is stated in the variable's own units as \emph{mean minus mode} —
a value of \code{0.3} on inflation means the risks are worth 0.3
percentage points to the upside. Skews may be given for any subset of
variables and horizons; anything unstated keeps symmetric bands.

Unlike \code{\link[=add_judgment]{add_judgment()}}, which moves the projection itself and
back-solves the shocks that support it, this changes only the shape of
the distribution around an unchanged central path.
}
\examples{
sol <- qpm_solve(qpm_template("bkl"))
fc <- qpm_forecast(sol, horizon = 8)
risky <- qpm_risk(fc, pi = 0.4, author = "MPC",
                  rationale = "energy prices tilted to the upside")
risky
plot(risky, vars = c("pi", "i"))
}
