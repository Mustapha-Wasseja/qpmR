% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R
\name{vcov.qpm_estimate}
\alias{vcov.qpm_estimate}
\alias{confint.qpm_estimate}
\title{Posterior covariance and credible intervals}
\usage{
\method{vcov}{qpm_estimate}(object, ...)

\method{confint}{qpm_estimate}(object, parm = NULL, level = 0.9, ...)
}
\arguments{
\item{object}{A \code{qpm_estimate}.}

\item{...}{Unused.}

\item{parm}{Parameters to report; default all.}

\item{level}{Credible level.}
}
\value{
\code{vcov()} returns the posterior covariance matrix of the
estimated parameters; \code{confint()} returns equal-tailed posterior
credible intervals (posterior quantiles, not asymptotic intervals).
}
\description{
Posterior covariance and credible intervals
}
\examples{
\donttest{
m <- qpm_model(variables = vars(x = "x"), shocks = shocks(e),
               equations = eqs(x ~ rho * x[-1] + e),
               params = list(rho = 0.5))
obs <- simulate(qpm_solve(qpm_calibrate(m, rho = 0.8)), nsim = 120, seed = 1)
est <- qpm_estimate(m, obs, priors(rho = beta(0.5, 0.2)),
                    iter = 600, chains = 2, seed = 2, verbose = FALSE)
vcov(est)
confint(est)
}
}
