% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsl-model.R
\name{qpm_model}
\alias{qpm_model}
\title{Define a quarterly projection model}
\usage{
qpm_model(
  name = "QPM model",
  variables,
  shocks,
  equations,
  params = list(),
  sigma = NULL,
  meta = list()
)
}
\arguments{
\item{name}{Model name used in printed output.}

\item{variables}{Variable declarations from \code{\link[=vars]{vars()}}.}

\item{shocks}{Shock declarations from \code{\link[=shocks]{shocks()}}.}

\item{equations}{Equations from \code{\link[=eqs]{eqs()}}; one per variable.}

\item{params}{Named list/vector of parameter values.}

\item{sigma}{Named vector of shock standard deviations. Defaults to 1
for every shock.}

\item{meta}{Optional list of metadata (e.g. plausible parameter
\code{ranges} used by \code{\link[=qpm_lint]{qpm_lint()}}).}
}
\value{
An object of class \code{qpm_model}.
}
\description{
Declares a linear semi-structural model with model-consistent
expectations. Equations are parsed immediately, so unknown symbols and
malformed lag/lead references are caught at construction time.
Coefficients are extracted from the current calibration when the model
is solved with \code{\link[=qpm_solve]{qpm_solve()}}.
}
\examples{
m <- qpm_model(
  name      = "AR(1) toy",
  variables = vars(x = "A persistent process"),
  shocks    = shocks(eps_x),
  equations = eqs(x ~ rho * x[-1] + eps_x),
  params    = list(rho = 0.8)
)
m
}
