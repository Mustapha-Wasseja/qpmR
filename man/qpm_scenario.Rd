% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/condition.R
\name{qpm_scenario}
\alias{qpm_scenario}
\title{Shock-based alternative scenarios}
\usage{
qpm_scenario(fc, shocks, anticipated = FALSE, label = NULL)
}
\arguments{
\item{fc}{A \code{qpm_forecast}.}

\item{shocks}{Named list: one entry per shock, each a named vector of
shock sizes (raw equation units) by period, e.g.
\code{list(eps_q = c(h1 = 1.5))}.}

\item{anticipated}{Announced at start (\code{TRUE}) or surprises (\code{FALSE}).}

\item{label}{Optional scenario label for printing.}
}
\value{
The shifted \code{qpm_forecast} (bands unchanged: a deterministic
scenario shifts the mean, not the uncertainty).
}
\description{
Adds a specified path of structural shocks to a forecast -- "what if
oil pushes the exchange rate 10 percent weaker next quarter?" --
without any inversion. Under \code{anticipated = TRUE} the shock path is
announced at the start of the forecast and expectations react ahead
of it.
}
\examples{
sol <- qpm_solve(qpm_template("bkl"))
fc <- qpm_forecast(sol, horizon = 8)
dep <- qpm_scenario(fc, shocks = list(eps_q = c(h1 = 3)),
                    label = "10pct depreciation")
}
