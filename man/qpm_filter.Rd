% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter.R
\name{qpm_filter}
\alias{qpm_filter}
\title{Estimate latent states from data (Kalman filter/smoother)}
\usage{
qpm_filter(x, data, observables = NULL, measurement_error = 0, kappa = 1e+06)
}
\arguments{
\item{x}{A \code{qpm_model} (solved internally) or \code{qpm_solution}.}

\item{data}{A data frame in levels (model units). Columns whose names
match declared variables are used as observables; an optional
\code{period} column provides labels. \code{NA}s are allowed anywhere.}

\item{observables}{Optional character vector restricting which columns
are treated as observed.}

\item{measurement_error}{Measurement-error standard deviation(s):
scalar or named vector over observables. Defaults to 0.}

\item{kappa}{Diffuse-prior variance scale used when the model has
unit-root (random-walk) trends; see \code{\link[=state_space]{state_space()}}.}
}
\value{
An object of class \code{qpm_filtration}: smoothed states in levels
(\verb{$states}), their standard errors (\verb{$se}), smoothed structural
shocks (\verb{$shocks}), the log-likelihood (\verb{$loglik}), innovation
diagnostics (\verb{$diag}), and the full expanded-state matrix
(\verb{$states_dev}). Feed it to \code{\link[=qpm_decompose]{qpm_decompose()}} for historical shock
decompositions or to \code{\link[=qpm_forecast]{qpm_forecast()}} to forecast from the smoothed
current state.
}
\description{
Runs the Kalman filter and RTS smoother over the solved model, jointly
inferring every latent variable — output gap, neutral rate, equilibrium
exchange rate, trend processes — and the historical structural shocks
from whatever subset of variables you actually observe. Missing values
(ragged edges, gappy series) are handled naturally.
}
\details{
The filter is initialized at the model's stationary distribution
(Lyapunov covariance), which is exact for the stationary models qpmR
currently supports.
}
\examples{
sol <- qpm_solve(qpm_template("bkl"))
obs <- simulate(sol, nsim = 60, seed = 3, burn = 20)
fit <- qpm_filter(sol, obs[, c("period", "pi", "i", "q")])
fit
plot(fit, vars = c("y_gap", "r_bar"))
}
