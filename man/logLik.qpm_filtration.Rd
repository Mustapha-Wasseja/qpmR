% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R
\name{logLik.qpm_filtration}
\alias{logLik.qpm_filtration}
\alias{logLik.qpm_estimate}
\title{Log-likelihood of a filtration or an estimate}
\usage{
\method{logLik}{qpm_filtration}(object, ...)

\method{logLik}{qpm_estimate}(object, ...)
}
\arguments{
\item{object}{A \code{qpm_filtration} or \code{qpm_estimate}.}

\item{...}{Unused.}
}
\value{
An object of class \code{logLik}.
}
\description{
For a \code{qpm_filtration} this is the Kalman-filter log-likelihood of
the data under the calibrated model, with zero degrees of freedom
(nothing was estimated). For a \code{qpm_estimate} it is the
log-likelihood at the posterior mode (or at the maximum for
\code{method = "mle"}), with degrees of freedom equal to the number of
estimated parameters — so \code{\link[stats:AIC]{stats::AIC()}} and \code{\link[stats:AIC]{stats::BIC()}} work.
}
\examples{
sol <- qpm_solve(qpm_template("bkl"))
obs <- simulate(sol, nsim = 40, seed = 1, burn = 20)
fit <- qpm_filter(sol, obs[, c("period", "pi", "i", "q")])
logLik(fit)
}
