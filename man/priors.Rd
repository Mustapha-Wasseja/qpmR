% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priors.R
\name{priors}
\alias{priors}
\title{Declare priors for Bayesian estimation}
\usage{
priors(...)
}
\arguments{
\item{...}{Named prior declarations, e.g.
\verb{b1 = beta(0.7, 0.1), c2 = truncate(normal(1.5, 0.25), lower = 1)}.}
}
\value{
An object of class \code{qpm_priors}.
}
\description{
One named argument per estimated quantity: a structural parameter name
or a shock name (meaning that shock's standard deviation). Everything
without a prior stays calibrated.
}
\details{
Available distributions (usable only inside \code{priors()}, so base R's
\code{beta()} and \code{gamma()} functions are never masked):
\itemize{
\item \code{normal(mean, sd)}
\item \code{beta(mean, sd)} — on (0, 1), mean/sd parametrization
\item \code{gamma(mean, sd)} — on (0, Inf)
\item \code{invgamma(mean, sd)} — on (0, Inf); the usual choice for shock sds
\item \code{uniform(min, max)}
\item \code{truncate(d, lower, upper)} — restrict any of the above; the
normalizing constant is dropped (harmless for modes and MCMC)
}
}
\examples{
p <- priors(
  b1 = beta(0.7, 0.1),
  b2 = gamma(0.25, 0.1),
  c2 = truncate(normal(1.5, 0.25), lower = 1),
  eps_pi = invgamma(1, 0.3)
)
p
}
