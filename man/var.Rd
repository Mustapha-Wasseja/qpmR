% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsl-vars.R
\name{var}
\alias{var}
\title{Declare a model variable with a label and unit}
\usage{
var(label = "", unit = "", ...)
}
\arguments{
\item{label}{Human-readable description, e.g. \code{"Output gap"} (or
numeric data, which is passed to \code{\link[stats:cor]{stats::var()}}).}

\item{unit}{Unit of measurement, e.g. \code{"pp"}, \code{"pct pa"}.}

\item{...}{Passed to \code{\link[stats:cor]{stats::var()}} in the delegation case.}
}
\value{
An object of class \code{qpm_var}, or the result of \code{\link[stats:cor]{stats::var()}}.
}
\description{
Used inside \code{\link[=vars]{vars()}} to attach documentation to a variable. Units are
stored and displayed; unit algebra (automatic consistency checking
inside equations) is on the roadmap.
}
\details{
When given numeric data instead of a character label, \code{var()}
delegates to \code{\link[stats:cor]{stats::var()}}, so attaching qpmR does not break variance
computations.
}
\examples{
vars(y_gap = var("Output gap", unit = "pp"))
var(rnorm(10))  # still the sample variance
}
