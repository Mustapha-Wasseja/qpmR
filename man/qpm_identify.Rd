% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/identify.R
\name{qpm_identify}
\alias{qpm_identify}
\title{Identification diagnostics (Iskrev-style Jacobian analysis)}
\usage{
qpm_identify(model, params = NULL, observables = NULL, lags = 3, h = 1e-05)
}
\arguments{
\item{model}{A \code{qpm_model}.}

\item{params}{Parameters to check: a character vector of structural
parameter and/or shock names, or a \code{\link[=priors]{priors()}} object (its names are
used). Default: all structural parameters.}

\item{observables}{Observed variables the moment analysis conditions
on. Default: all declared variables.}

\item{lags}{Autocovariance lags in the moment vector.}

\item{h}{Relative step for the central differences.}
}
\value{
An object of class \code{qpm_identification} with the ranks,
singular values, and flagged parameters; printed as a verdict list.
}
\description{
Checks, before any estimation is run, whether the chosen parameters
can be told apart by the data. Two Jacobians are analyzed numerically
at the current calibration, in the spirit of Iskrev (2010):
}
\details{
\itemize{
\item \strong{solution level}: derivatives of the solved transition, shock
loading, and observable steady state with respect to the
parameters. Rank deficiency here means some parameter movements do
not change the model's solution at all.
\item \strong{moment level} (stationary models only): derivatives of the
observables' first and second moments (means, and autocovariances
up to \code{lags}). Rank deficiency here means some parameter movements
are observationally equivalent in population.
}

The report names parameters with (numerically) no effect, parameter
combinations spanning any null space, and near-collinear pairs of
Jacobian columns (correlation above 0.995) that are only jointly
identified.
}
\examples{
qpm_identify(qpm_template("bkl"),
             params = c("b1", "b2", "b3", "c1", "c2"),
             observables = c("pi", "i", "q", "dy_obs"))
}
\references{
Iskrev, N. (2010). Local identification in DSGE models.
Journal of Monetary Economics, 57(2), 189-202.
}
