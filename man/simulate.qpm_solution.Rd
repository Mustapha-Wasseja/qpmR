% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate.qpm_solution}
\alias{simulate.qpm_solution}
\title{Simulate a solved model}
\usage{
\method{simulate}{qpm_solution}(object, nsim = 40, seed = NULL, sigma = NULL, burn = 0, ...)
}
\arguments{
\item{object}{A \code{qpm_solution}.}

\item{nsim}{Number of quarters to simulate.}

\item{seed}{Optional seed for reproducibility.}

\item{sigma}{Optional named vector of shock standard deviations
overriding the model's.}

\item{burn}{Burn-in quarters discarded from the start.}

\item{...}{Unused.}
}
\value{
A data frame of class \code{qpm_sim} (period plus one column per
declared variable), with attributes \code{state} (final expanded state,
deviations) and \code{shocks} (the drawn shocks).
}
\description{
Draws Gaussian shocks with the model's standard deviations and iterates
the solved transition. Returns variables in levels (steady state plus
simulated deviations). The final full state is stored as an attribute
so a simulation can be handed straight to \code{\link[=qpm_forecast]{qpm_forecast()}}.
}
\examples{
sol <- qpm_solve(qpm_template("bkl"))
hist <- simulate(sol, nsim = 60, seed = 42, burn = 20)
head(hist)
}
