% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_models.R
\name{qpm_compare_models}
\alias{qpm_compare_models}
\alias{plot.qpm_model_comparison}
\title{Compare the behaviour of two or more models}
\usage{
qpm_compare_models(models, shock = NULL, vars = NULL, horizon = 20)

\method{plot}{qpm_model_comparison}(x, vars = NULL, ...)
}
\arguments{
\item{models}{A named list of \code{qpm_model} or \code{qpm_solution} objects.}

\item{shock}{Shock whose impulse responses are compared. Default: the
first shock common to every model.}

\item{vars}{Variables to compare. Default: those common to all models,
capped at the usual headline set.}

\item{horizon}{Impulse-response horizon.}

\item{x}{A \code{qpm_model_comparison}.}

\item{...}{Unused.}
}
\value{
An object of class \code{qpm_model_comparison} holding the
impulse responses and, for stationary models, the implied moments.
}
\description{
\code{\link[=qpm_diff]{qpm_diff()}} compares model \emph{structure} — what a country team changed
in the code. This compares model \emph{behaviour}: the transmission of a
given shock and the moments each specification implies. When a
calibration is revised, both questions matter, and the second is the
one a policy audience asks.
}
\examples{
base <- qpm_template("bkl")
flat <- qpm_calibrate(base, b2 = 0.05)   # a much flatter Phillips curve
cmp <- qpm_compare_models(list(baseline = base, flat = flat),
                          shock = "eps_y")
cmp
plot(cmp, vars = c("pi", "i"))
}
