% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate.R
\name{qpm_estimate}
\alias{qpm_estimate}
\alias{coef.qpm_estimate}
\title{Estimate model parameters (Bayesian or maximum likelihood)}
\usage{
qpm_estimate(
  model,
  data,
  priors,
  observables = NULL,
  measurement_error = 0,
  kappa = 1e+06,
  method = c("bayes", "mle"),
  iter = 4000,
  burn = floor(iter/2),
  chains = 2,
  thin = 1,
  seed = NULL,
  verbose = TRUE
)

\method{coef}{qpm_estimate}(object, type = c("mean", "mode", "median"), ...)
}
\arguments{
\item{model}{A \code{qpm_model}.}

\item{data}{Data frame in levels (as for \code{\link[=qpm_filter]{qpm_filter()}}).}

\item{priors}{A \code{\link[=priors]{priors()}} declaration. Names are structural
parameters or shock names (meaning that shock's sd). Required for
\code{method = "bayes"}; for \code{"mle"} it supplies supports (and is
otherwise ignored).}

\item{observables, measurement_error, kappa}{Passed to the filter.}

\item{method}{\code{"bayes"} (default) or \code{"mle"}.}

\item{iter}{MCMC iterations per chain (including burn-in).}

\item{burn}{Burn-in iterations (default \code{iter/2}).}

\item{chains}{Number of chains (run sequentially).}

\item{thin}{Keep every \code{thin}-th post-burn draw.}

\item{seed}{Optional RNG seed.}

\item{verbose}{Print progress.}

\item{object}{A \code{qpm_estimate}.}

\item{type}{Point estimate: posterior \code{"mean"}, \code{"mode"}, or \code{"median"}.}

\item{...}{Unused.}
}
\value{
An object of class \code{qpm_estimate}: posterior \code{draws} (natural
units), the \code{mode}, acceptance rate, split R-hat and effective
sample sizes, and the originating model/data. Use \code{\link[=coef]{coef()}} to
extract point estimates and \code{\link[=apply_estimate]{apply_estimate()}} to recalibrate the
model.
}
\description{
Estimates any subset of structural parameters and shock standard
deviations from data, using the Kalman-filter likelihood of the solved
model. \code{method = "bayes"} finds the posterior mode (in transformed,
unconstrained space), seeds an adaptive random-walk Metropolis sampler
with the inverse Hessian, and returns posterior draws with split
R-hat and effective-sample-size diagnostics. \code{method = "mle"} uses the
same machinery with flat priors on the declared supports.
}
\details{
Draws that violate Blanchard-Kahn (indeterminacy or explosiveness)
receive zero posterior weight — the usual truncation of the prior to
the determinacy region. Everything without a prior stays calibrated
at its current value.
}
\examples{
\donttest{
m <- qpm_model(variables = vars(x = "x"), shocks = shocks(e),
               equations = eqs(x ~ rho * x[-1] + e),
               params = list(rho = 0.5))
obs <- simulate(qpm_solve(qpm_calibrate(m, rho = 0.8)), nsim = 200, seed = 1)
est <- qpm_estimate(m, obs, priors(rho = beta(0.5, 0.2), e = invgamma(1, 0.3)),
                    iter = 1000, chains = 2, seed = 2, verbose = FALSE)
est
}
}
