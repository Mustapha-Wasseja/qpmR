% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dynare.R
\name{write_dynare}
\alias{write_dynare}
\title{Export a model to a Dynare .mod file}
\usage{
write_dynare(model, file = "model.mod", irf = 40, order = 1, extra = NULL)
}
\arguments{
\item{model}{A \code{qpm_model}.}

\item{file}{Output path. \code{NULL} returns the text instead of writing.}

\item{irf}{Horizon for \code{stoch_simul}; \code{0} omits impulse responses.}

\item{order}{Approximation order passed to \code{stoch_simul} (the models
are linear, so first order is exact).}

\item{extra}{Character vector of extra Dynare statements appended
after \code{stoch_simul}, e.g. code to export results.}
}
\value{
The file path invisibly, or the Dynare source as a character
vector when \code{file} is \code{NULL}.
}
\description{
Writes the model as Dynare source so that anyone can re-solve it in
Dynare and compare. This is deliberate: qpmR's solver is pinned in its
own tests to analytic solutions, but "does it agree with Dynare?" is
the first question a modeller asks, and the answer should be cheap to
check rather than something to take on trust.
}
\details{
The original equations are exported, not qpmR's internal first-order
system: Dynare creates its own auxiliary variables for long leads and
lags, so the two implementations agree only if both handle them
correctly. Expectation wrappers are dropped (\code{E(pi[+1])} becomes
\code{pi(+1)}), since leads in Dynare are already model-consistent
expectations.
}
\examples{
src <- write_dynare(qpm_template("bkl"), file = NULL)
cat(head(src, 15), sep = "\n")
}
