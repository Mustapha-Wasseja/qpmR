% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chart_pack.R
\name{chart_pack}
\alias{chart_pack}
\title{The standard forecast-round chart pack}
\usage{
chart_pack(
  round,
  file = NULL,
  charts = NULL,
  vars = NULL,
  width = 9,
  height = 6.5
)
}
\arguments{
\item{round}{A \code{qpm_round}.}

\item{file}{Output file. A \code{.pdf} extension gives one multi-page
document; a \code{.png} extension gives numbered files
(\verb{name-1.png}, ...). \code{NULL} draws to the current device.}

\item{charts}{Which charts to include, from \code{"forecast"}, \code{"gaps"},
\code{"decomposition"}, \code{"transmission"}. Default: all four.}

\item{vars}{Variables for the forecast page. Default: the model's
headline set, whichever of \code{pi4}/\code{pi}, \code{i}, \code{y_gap}, \code{q} exist.}

\item{width, height}{Page size in inches.}
}
\value{
The output path (invisibly), or \code{NULL} when drawing to the
current device.
}
\description{
Produces the chart set a forecast round is discussed from: the
forecast fans, the filtered latent states, the historical shock
decomposition of inflation, and the model's monetary transmission.
Output is a multi-page PDF by default, or numbered PNGs.
}
\examples{
m <- qpm_template("bkl")
obs <- simulate(qpm_solve(m), nsim = 40, seed = 1, burn = 20)
obs$period <- next_quarters("2016-Q1", 40)
r <- qpm_round("demo", m, obs[, c("period", "pi", "i", "q")], horizon = 8)
pdf_path <- file.path(tempdir(), "pack.pdf")
chart_pack(r, pdf_path)
}
