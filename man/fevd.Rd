% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fevd.R
\name{fevd}
\alias{fevd}
\alias{fevd.qpm_solution}
\alias{plot.qpm_fevd}
\title{Forecast error variance decomposition}
\usage{
fevd(x, ...)

\method{fevd}{qpm_solution}(x, horizon = 24, vars = NULL, shocks = NULL, ...)

\method{plot}{qpm_fevd}(x, var = NULL, drop_zero = TRUE, ...)
}
\arguments{
\item{x}{A \code{qpm_solution}, or a \code{qpm_model} (solved first).}

\item{...}{Passed to methods.}

\item{horizon}{Largest forecast horizon.}

\item{vars}{Variables to include; default all declared variables.}

\item{shocks}{Shocks to include; default all.}

\item{var}{Variable to plot.}

\item{drop_zero}{Omit shocks that never contribute.}
}
\value{
A data frame of class \code{qpm_fevd} in long format with columns
\code{variable}, \code{shock}, \code{horizon}, \code{share} and \code{variance}.
\code{print()} shows the dominant shocks; \code{plot()} draws stacked shares.
}
\description{
Splits the forecast error variance of each variable at each horizon
into the contributions of the structural shocks — "how much of
inflation uncertainty two years out is the cost-push shock?". For
\deqn{x_t = P x_{t-1} + Q e_t,  e_t ~ N(0, S)}
the h-step forecast error variance is
\deqn{V_h = sum_{j<h} P^j Q S Q' (P^j)'}
and the share of shock \code{k} is the same sum with only column \code{k} of
\code{Q} active.
Shares sum to one across shocks for every variable and horizon.
}
\details{
Shares remain well defined when the model has unit roots, even though
the variances themselves grow without bound.
}
\examples{
sol <- qpm_solve(qpm_template("bkl"))
fv <- fevd(sol, horizon = 20)
fv
plot(fv, var = "pi")
}
