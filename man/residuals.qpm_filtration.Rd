% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R
\name{residuals.qpm_filtration}
\alias{residuals.qpm_filtration}
\alias{fitted.qpm_filtration}
\title{One-step-ahead prediction errors and fitted values}
\usage{
\method{residuals}{qpm_filtration}(object, type = c("innovation", "standardized", "shock"), ...)

\method{fitted}{qpm_filtration}(object, ...)
}
\arguments{
\item{object}{A \code{qpm_filtration}.}

\item{type}{Which residuals to return.}

\item{...}{Unused.}
}
\value{
A data frame with a \code{period} column and one column per series.
}
\description{
\code{residuals()} returns the filter's one-step-ahead prediction errors
for the observed series (\code{type = "innovation"}), the same divided by
their standard deviations (\code{"standardized"}, which is what the
outlier flags use), or the smoothed structural shocks
(\code{"shock"}). \code{fitted()} returns the one-step-ahead predictions of the
observables, so that \code{observed = fitted + innovation}.
}
\examples{
sol <- qpm_solve(qpm_template("bkl"))
obs <- simulate(sol, nsim = 40, seed = 1, burn = 20)
fit <- qpm_filter(sol, obs[, c("period", "pi", "i", "q")])
head(residuals(fit, "standardized"))
head(fitted(fit))
}
