% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decompose.R
\name{qpm_decompose}
\alias{qpm_decompose}
\alias{plot.qpm_decomposition}
\title{Historical shock decomposition}
\usage{
qpm_decompose(fit, vars = NULL)

\method{plot}{qpm_decomposition}(x, var = NULL, drop_zero = TRUE, periods = NULL, ...)
}
\arguments{
\item{fit}{A \code{qpm_filtration} from \code{\link[=qpm_filter]{qpm_filter()}}.}

\item{vars}{Variables to keep (default: all declared variables).}

\item{x}{A \code{qpm_decomposition}.}

\item{var}{Variable to plot.}

\item{drop_zero}{Drop components that never contribute.}

\item{periods}{Optional integer window of period indices to display
(e.g. \code{81:110} for the last 30 quarters).}

\item{...}{Unused.}
}
\value{
A long data frame of class \code{qpm_decomposition} with columns
\code{period}, \code{variable}, \code{component} (shock names plus \code{"initial"}),
and \code{value} (contribution, in deviations from steady state).
\code{plot()} draws a stacked-bar decomposition with the smoothed total
overlaid.
}
\description{
Splits the smoothed history of every variable into the additive
contributions of each structural shock plus the carry-over of the
pre-sample initial state: with smoothed shocks \eqn{e_t},
\deqn{a_t = sum_j c_j(t) + c_0(t),  c_j(t) = P c_j(t-1) + Q_j e_{j,t}}
The contributions sum exactly to the smoothed state (deviations from
steady state); this identity is verified internally.
}
\examples{
sol <- qpm_solve(qpm_template("bkl"))
obs <- simulate(sol, nsim = 60, seed = 3, burn = 20)
fit <- qpm_filter(sol, obs[, c("period", "pi", "i", "q")])
dec <- qpm_decompose(fit)
plot(dec, var = "pi")
}
