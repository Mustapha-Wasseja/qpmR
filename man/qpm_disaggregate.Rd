% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/disaggregate.R
\name{qpm_disaggregate}
\alias{qpm_disaggregate}
\alias{plot.qpm_disaggregation}
\title{Temporal disaggregation of low-frequency data}
\usage{
qpm_disaggregate(
  annual,
  indicator = NULL,
  frequency = 4,
  method = c("denton", "chow-lin"),
  conversion = c("sum", "average"),
  rho = NULL
)

\method{plot}{qpm_disaggregation}(x, ...)
}
\arguments{
\item{annual}{Numeric vector of low-frequency values.}

\item{indicator}{Optional numeric vector of high-frequency indicator
values, length \code{frequency * length(annual)}. Required for
\code{"chow-lin"}.}

\item{frequency}{Periods per low-frequency observation (4 for
annual-to-quarterly).}

\item{method}{\code{"denton"} or \code{"chow-lin"}.}

\item{conversion}{\code{"sum"} or \code{"average"}.}

\item{rho}{AR(1) coefficient for \code{"chow-lin"}. \code{NULL} estimates it by
a grid search on the profile GLS likelihood. Note that \code{rho} is
identified only from the \emph{low-frequency} residuals, so a short
sample cannot pin it down: with ten annual observations the estimate
is typically driven to zero even when the quarterly residual is
strongly autocorrelated. Around thirty low-frequency observations
are needed before the estimate is informative; supply \code{rho}
directly when the sample is shorter.}

\item{x}{A \code{qpm_disaggregation}.}

\item{...}{Unused.}
}
\value{
An object of class \code{qpm_disaggregation}: the high-frequency
series, the method used and the fitted parameters.
}
\description{
Turns annual series into quarterly ones consistent with the annual
totals. Many of the economies these models are built for publish
national accounts annually, so a quarterly projection model has to
start by constructing quarterly GDP — usually from an indicator such
as industrial production, imports or credit.
}
\details{
Two standard methods:
\itemize{
\item \code{"denton"} — Denton-Cholette proportional first differences.
Minimises the squared change in the ratio of the quarterly series to
the indicator (or, without an indicator, in the series itself),
subject to matching the annual figures. Purely a smoothing method:
no regression, no parameters.
\item \code{"chow-lin"} — generalised least squares on the indicator with
AR(1) quarterly residuals, distributing the annual residual across
quarters. Uses the indicator's regression relationship, so it is the
better choice when the indicator genuinely tracks the target.
}

Both enforce the aggregation constraint exactly: \code{"sum"} for flows
(annual GDP is the sum of quarters), \code{"average"} for stocks and index
levels.
}
\examples{
# annual GDP with a quarterly indicator
set.seed(1)
q_true <- cumsum(rnorm(40, 0.5)) + 100
annual <- colSums(matrix(q_true, nrow = 4))
ind <- q_true + rnorm(40, 0, 1)
d <- qpm_disaggregate(annual, ind, method = "chow-lin")
d
plot(d)
}
\references{
Denton, F. T. (1971); Chow, G. C. and Lin, A. (1971).
}
