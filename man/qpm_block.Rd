% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blocks.R
\name{qpm_block}
\alias{qpm_block}
\title{Model extension blocks}
\usage{
qpm_block(
  name,
  variables = NULL,
  shocks = NULL,
  params = list(),
  sigma = NULL,
  equations = NULL,
  description = ""
)
}
\arguments{
\item{name}{Block name, used in printing and in the model's history.}

\item{variables}{New variables, from \code{\link[=vars]{vars()}} (optional).}

\item{shocks}{New shocks, from \code{\link[=shocks]{shocks()}} (optional).}

\item{params}{Named list of new parameters (optional).}

\item{sigma}{Named vector of standard deviations for the new shocks
(optional; default 1).}

\item{equations}{Equations, from \code{\link[=eqs]{eqs()}}: replacements for existing
variables and definitions for new ones.}

\item{description}{One-line description of what the block does.}
}
\value{
An object of class \code{qpm_block}.
}
\description{
A block is a documented, reusable bundle of variables, shocks,
parameters and equations that adapts a template to a country. Blocks
are how a technical-assistance engagement customizes the canonical
model without forking it: \code{\link[=add_block]{add_block()}} applies one, \code{\link[=qpm_diff]{qpm_diff()}}
shows exactly what changed, and the result is an ordinary
\code{qpm_model}.
}
\details{
Equations whose left-hand side names a variable that already exists
\strong{replace} that variable's equation; equations for newly declared
variables are appended. Left-hand sides must be bare variable names.
}
\examples{
b <- qpm_block(
  name = "risk premium shifter",
  params = list(prem_extra = 0),
  equations = eqs(prem ~ rho_prem * prem[-1] +
                    (1 - rho_prem) * (prem_ss + prem_extra) + eps_prem)
)
m <- add_block(qpm_template("bkl"), b)
}
\seealso{
\code{\link[=block_food_cpi]{block_food_cpi()}}, \code{\link[=block_fx_intervention]{block_fx_intervention()}}
}
