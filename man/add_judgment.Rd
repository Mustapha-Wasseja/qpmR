% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/judgment.R
\name{add_judgment}
\alias{add_judgment}
\title{Add logged judgment to a forecast}
\usage{
add_judgment(
  fc,
  ...,
  author = "desk",
  rationale = "",
  anticipated = NULL,
  instruments = NULL
)
}
\arguments{
\item{fc}{A \code{qpm_forecast}.}

\item{...}{Named adjustments: one argument per variable, each a named
vector of \emph{additions} (percentage points, relative to the current
forecast) by period, e.g. \code{pi = c("2027-Q1" = 0.4)}.}

\item{author}{Who is imposing the judgment (logged).}

\item{rationale}{Why (logged; make it meaningful -- the ledger is the
audit trail read back before the policy meeting).}

\item{anticipated}{Expectation mode for the re-solve; defaults to the
forecast's current mode, or unanticipated.}

\item{instruments}{Shocks allowed to move; defaults to the forecast's
current instruments, or all shocks.}
}
\value{
The adjusted \code{qpm_forecast} with the entry appended to its
judgment ledger.
}
\description{
Central-bank forecasts are never raw model output: the desk knows
about the announced electricity tariff, the tax change, the one-off
the model cannot see. \code{add_judgment()} makes that adjustment a
first-class, logged operation: you state the change you want (in
percentage points, relative to the current forecast), qpmR back-solves
the structural shocks needed to support it while keeping the whole
forecast model-consistent, records who imposed it and why, and flags
judgment that requires implausibly large shocks.
}
\details{
Judgment entries are stored as absolute targets, so the ledger is
replayable; the full set of conditions and judgment is re-solved
jointly each time. Inspect the ledger with \code{\link[=judgment_log]{judgment_log()}}.
}
\examples{
sol <- qpm_solve(qpm_template("bkl"))
fc <- qpm_forecast(sol, horizon = 8)
fc2 <- add_judgment(fc, pi = c(h2 = 0.5),
                    author = "desk",
                    rationale = "announced electricity tariff increase")
judgment_log(fc2)
}
