% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsl-vars.R
\name{eqs}
\alias{eqs}
\title{Declare model equations}
\usage{
eqs(...)
}
\arguments{
\item{...}{Two-sided formulas.}
}
\value{
A list of formulas.
}
\description{
Each equation is a two-sided formula. Lags and leads use index
notation: \code{x[-1]} is the first lag, \code{x[+1]} the first lead. Wrap
expectations in \code{E()}: \code{E(pi[+1])} is the model-consistent expectation
of next quarter's inflation. Longer lags and leads (e.g. \code{E(pi4[+4])})
are handled automatically via auxiliary state variables.
}
\examples{
eqs(
  pi ~ b1 * pi[-1] + (1 - b1) * E(pi[+1]) + b2 * y_gap + eps_pi
)
}
