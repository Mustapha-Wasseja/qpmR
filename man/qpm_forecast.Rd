% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forecast.R
\name{qpm_forecast}
\alias{qpm_forecast}
\title{Model forecast with uncertainty bands}
\usage{
qpm_forecast(
  object,
  from = NULL,
  horizon = 12,
  bands = c(0.5, 0.7, 0.9),
  sigma = NULL
)
}
\arguments{
\item{object}{A \code{qpm_solution}.}

\item{from}{Initial state: a \code{qpm_filtration} from \code{\link[=qpm_filter]{qpm_filter()}} (the
smoothed end-of-sample state is used and the smoothed history is kept
for plotting), a \code{qpm_sim} from \code{\link[=simulate]{simulate()}}, a full named deviation
vector over \code{object$vars_all}, or \code{NULL} (steady state).}

\item{horizon}{Forecast horizon in quarters.}

\item{bands}{Coverage levels for the fan, e.g. \code{c(0.5, 0.7, 0.9)}.}

\item{sigma}{Optional named vector of shock standard deviations.}
}
\value{
An object of class \code{qpm_forecast}: a list with \code{paths} (long
data frame: \code{variable}, \code{h}, \code{mean}, and \verb{lo_*}/\verb{hi_*} per band, in
levels), forecast-period labels in \verb{$periods}, plus the machinery
needed for conditioning.
}
\description{
Iterates the solved model forward from an initial state and computes
analytic forecast uncertainty from the shock variances,
\deqn{V_h = P V_{h-1} P' + Q S Q'}
giving Gaussian fan bands around the mean path. The result can then be
conditioned on assumed paths with \code{\link[=qpm_condition]{qpm_condition()}}, shifted by shock
scenarios with \code{\link[=qpm_scenario]{qpm_scenario()}}, or adjusted with logged judgment via
\code{\link[=add_judgment]{add_judgment()}}.
}
\examples{
sol <- qpm_solve(qpm_template("bkl"))
histq <- simulate(sol, nsim = 40, seed = 7, burn = 20)
fc <- qpm_forecast(sol, from = histq, horizon = 12)
fc
plot(fc, vars = c("pi", "i", "y_gap", "q"))
}
